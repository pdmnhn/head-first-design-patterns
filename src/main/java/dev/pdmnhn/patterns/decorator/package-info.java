/**
 * Design Principles,
 * <p>
 * 1. The Open-Closed Principle: Classes should be open for extension, but closed for modification.
 * <p>
 * Pattern used in this chapter: <b> Decorator Pattern</b>, The decorator pattern attaches additional responsibilities
 * to an object dynamically. Decorators provide a flexible alternative to subclassing for extending functionality.
 */
package dev.pdmnhn.patterns.decorator;

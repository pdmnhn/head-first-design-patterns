/**
 * Design Principles,
 * <p>
 * 1. Strive for loosely coupled designs between objects that interact.
 * <p>
 * Pattern used in this chapter: <b>Observer Pattern</b>, The Observer Pattern defines a one-to-many dependency between
 * objects so that when one object changes state, all of its dependents are notified and updated automatically.
 */
package dev.pdmnhn.patterns.observer;
